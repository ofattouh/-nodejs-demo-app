// lib/modules/redirect-login/index.js

module.exports = {
	construct: function(self, options) {
		
		/*		
  	self.loginAfterLogin = function(req) {
    	
	  	//console.log(req.user);
    	
    	if (req.user.username !== 'webmaster' && req.user.username !== 'vsampaiomaia' ) {
	    	//options.localLogin = false;
    	  req.redirect = '401-unauthorized-access-error';
  		} 
  		else {
    		// Just let them go go the home page
    	  //req.redirect = '/';
      }
    }; 
    */
    
  }
     
};
